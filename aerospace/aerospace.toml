# Place a copy of this config to ~/.aerospace.toml
# After that, you can edit ~/.aerospace.toml to your liking

# It's not necessary to copy all keys to your config.
# If the key is missing in your config, "default-config.toml" will serve as a fallback

# See https://nikitabobko.github.io/AeroSpace/guide#config-version
config-version = 2

# Workspaces that always exist, even when empty. config-version 1 inferred these
# from the workspace bindings below; version 2 needs them listed.
persistent-workspaces = ['1', '2', '3', '4', '5', '6', '7', '8', '9',
                         'A', 'C', 'D', 'F', 'G', 'M', 'O', 'P', 'R', 'S', 'T']

# You can use it to add commands that run after login to macOS user session.
# 'start-at-login' needs to be 'true' for 'after-login-command' to work
# Available commands: https://nikitabobko.github.io/AeroSpace/commands after-login-command = []

# You can use it to add commands that run after AeroSpace startup.
# 'after-startup-command' is run after 'after-login-command'
# Available commands : https://nikitabobko.github.io/AeroSpace/commands
# Experimental: answer window frame reads from WindowServer (~30x faster than AX)
skylight-reads = true

# Native window borders (replaces the external `borders` tool)
window-borders.enabled = true
window-borders.active-color = '0xffe1e3e4'
window-borders.inactive-color = '0xff494d64'
window-borders.width = 5
window-borders.corner-radius = 10
# Match system radii via NSThemeFrame probes (plain vs toolbar); no Screen Recording.
window-borders.detect-corner-radius = true

# Start AeroSpace at login
start-at-login = true

# Normalizations. See: https://nikitabobko.github.io/AeroSpace/guide#normalization
enable-normalization-flatten-containers = false
enable-normalization-opposite-orientation-for-nested-containers = false

# See: https://nikitabobko.github.io/AeroSpace/guide#layouts
# The 'accordion-padding' specifies the size of accordion padding
# You can set 0 to disable the padding feature
accordion-padding = 30

# Possible values: tiles|accordion
default-root-container-layout = 'tiles'

# Possible values: horizontal|vertical|auto
# 'auto' means: wide monitor (anything wider than high) gets horizontal orientation,
#               tall monitor (anything higher than wide) gets vertical orientation
default-root-container-orientation = 'auto'

# Possible values: (qwerty|dvorak)
# See https://nikitabobko.github.io/AeroSpace/guide#key-mapping
key-mapping.preset = 'qwerty'

# Mouse follows focus when focused monitor changes
# Drop it from your config, if you don't like this behavior
# See https://nikitabobko.github.io/AeroSpace/guide#on-focus-changed-callbacks
# See https://nikitabobko.github.io/AeroSpace/commands#move-mouse
on-focused-monitor-changed = ['move-mouse monitor-lazy-center']

# Gaps between windows (inner-*) and between monitor edges (outer-*).
# Possible values:
# - Constant:     gaps.outer.top = 8
# - Per monitor:  gaps.outer.top = [{ monitor.main = 16 }, { monitor."some-pattern" = 32 }, 24]
#                 In this example, 24 is a default value when there is no match.
#                 Monitor pattern is the same as for 'workspace-to-monitor-force-assignment'.
#                 See: https://nikitabobko.github.io/AeroSpace/guide#assign-workspaces-to-monitors
[gaps]
inner.horizontal = 16
inner.vertical =   16 
outer.left =       16 
outer.bottom =     16 
outer.top =        [{ monitor."ULTRAGEAR" = 40 }, { monitor.main = 16 }, 40]
outer.right =      16 

# 'main' binding mode declaration
# See: https://nikitabobko.github.io/AeroSpace/guide#binding-modes
# 'main' binding mode must be always presented
[mode.main.binding]

# All possible keys:
# - Letters.        a, b, c, ..., z
# - Numbers.        0, 1, 2, ..., 9
# - Keypad numbers. keypad0, keypad1, keypad2, ..., keypad9
# - F-keys.         f1, f2, ..., f20
# - Special keys.   minus, equal, period, comma, slash, backslash, quote, semicolon, backtick,
#                   leftSquareBracket, rightSquareBracket, space, enter, esc, backspace, tab
# - Keypad special. keypadClear, keypadDecimalMark, keypadDivide, keypadEnter, keypadEqual,
#                   keypadMinus, keypadMultiply, keypadPlus
# - Arrows.         left, down, up, right

# All possible modifiers: cmd, alt, ctrl, shift

# All possible commands: https://nikitabobko.github.io/AeroSpace/commands

# See: https://nikitabobko.github.io/AeroSpace/commands#exec-and-forget
alt-enter = '''exec-and-forget open -na Alacritty'''

# See: https://nikitabobko.github.io/AeroSpace/commands#layout
alt-slash = 'layout tiles horizontal vertical'
alt-comma = 'layout accordion horizontal vertical'

# See: https://nikitabobko.github.io/AeroSpace/commands#focus
alt-h = 'focus left'
alt-j = 'focus down'
alt-k = 'focus up'
alt-l = 'focus right'

# See: https://nikitabobko.github.io/AeroSpace/commands#move
alt-shift-h = 'move left'
alt-shift-j = 'move down'
alt-shift-k = 'move up'
alt-shift-l = 'move right'

# See: https://nikitabobko.github.io/AeroSpace/commands#resize
alt-shift-minus = 'resize smart -50'
alt-shift-equal = 'resize smart +50'
alt-shift-backspace = 'balance-sizes'

# See: https://nikitabobko.github.io/AeroSpace/commands#workspace
alt-1 = 'workspace 1'
alt-2 = 'workspace 2'
alt-3 = 'workspace 3'
alt-4 = 'workspace 4'
alt-5 = 'workspace 5'
alt-6 = 'workspace 6'
alt-7 = 'workspace 7'
alt-8 = 'workspace 8'
alt-9 = 'workspace 9'
alt-a = 'workspace A'
alt-c = 'workspace C'
alt-d = 'workspace D'
alt-f = 'workspace F'
alt-g = 'workspace G'
alt-m = 'workspace M'
alt-o = 'workspace O'
alt-r = 'workspace R'
alt-p = 'workspace P'
alt-t = 'workspace T'
alt-s = 'workspace S'

# See: https://nikitabobko.github.io/AeroSpace/commands#move-node-to-workspace
alt-shift-1 = 'move-node-to-workspace 1'
alt-shift-2 = 'move-node-to-workspace 2'
alt-shift-3 = 'move-node-to-workspace 3'
alt-shift-4 = 'move-node-to-workspace 4'
alt-shift-5 = 'move-node-to-workspace 5'
alt-shift-6 = 'move-node-to-workspace 6'
alt-shift-7 = 'move-node-to-workspace 7'
alt-shift-8 = 'move-node-to-workspace 8'
alt-shift-9 = 'move-node-to-workspace 9'
alt-shift-a = 'move-node-to-workspace A'
alt-shift-c = 'move-node-to-workspace C'
alt-shift-d = 'move-node-to-workspace D'
alt-shift-f = 'move-node-to-workspace F'
alt-shift-g = 'move-node-to-workspace G'
alt-shift-m = 'move-node-to-workspace M'
alt-shift-o = 'move-node-to-workspace O'
alt-shift-r = 'move-node-to-workspace R'
alt-shift-p = 'move-node-to-workspace P'
alt-shift-t = 'move-node-to-workspace T'
alt-shift-s = 'move-node-to-workspace S'

alt-shift-enter= 'fullscreen'

# See: https://nikitabobko.github.io/AeroSpace/commands#workspace-back-and-forth
alt-tab = 'workspace-back-and-forth'
# See: https://nikitabobko.github.io/AeroSpace/commands#move-workspace-to-monitor
alt-shift-tab = 'move-workspace-to-monitor --wrap-around next'

# Toggle between floating and tiling layout (i3's $mod+shift+space)
# Heads up: alt-shift-f is 'move-node-to-workspace F', don't confuse them
alt-shift-space = 'layout floating tiling && test %{window-layout} = floating && resize smart 60% && center-window'

# See: https://nikitabobko.github.io/AeroSpace/commands#mode
alt-shift-semicolon = ['mode service']

# 'service' binding mode declaration.
# See: https://nikitabobko.github.io/AeroSpace/guide#binding-modes
[mode.service.binding]
esc = ['reload-config', 'mode main']
r = ['flatten-workspace-tree', 'mode main'] # reset layout
#s = ['layout sticky tiling', 'mode main'] # sticky is not yet supported https://github.com/nikitabobko/AeroSpace/issues/2
f = ['layout floating tiling', 'mode main'] # Toggle between floating and tiling layout
backspace = ['close-all-windows-but-current', 'mode main']

alt-shift-h = ['join-with left', 'mode main']
alt-shift-j = ['join-with down', 'mode main']
alt-shift-k = ['join-with up', 'mode main']
alt-shift-l = ['join-with right', 'mode main']

# Floating apps
[[on-window-detected]]
if.app-id = 'com.apple.finder'
run = "layout floating"

[[on-window-detected]]
if.app-id = 'com.apple.ActivityMonitor'
run = "layout floating"

[[on-window-detected]]
if.app-id = 'org.sveinbjorn.Platypus'
run = "layout floating"

# Predefined workspaces

[[on-window-detected]]
if.app-id = 'app.zen-browser.zen'
check-further-callbacks = false
run = 'move-node-to-workspace 1'

[[on-window-detected]]
if.app-id = 'com.openai.chat'
check-further-callbacks = false
run = 'move-node-to-workspace A'

[[on-window-detected]]
if.app-name-regex-substring = 'Cursor'
check-further-callbacks = false
run = 'move-node-to-workspace C'

[[on-window-detected]]
if.app-id = 'com.hnc.Discord'
check-further-callbacks = false
run = 'move-node-to-workspace D'

[[on-window-detected]]
if.app-id = 'com.figma.Desktop'
check-further-callbacks = false
run = 'move-node-to-workspace F'

[[on-window-detected]]
if.app-id = 'com.jetbrains.datagrip'
check-further-callbacks = false
run = 'move-node-to-workspace G'

[[on-window-detected]]
if.app-id = 'com.tinyspeck.slackmacgap'
check-further-callbacks = false
run = 'move-node-to-workspace S'

[[on-window-detected]]
if.app-id = 'com.spotify.client'
check-further-callbacks = false
run = 'move-node-to-workspace M'

[[on-window-detected]]
if.app-id = 'com.jetbrains.rider'
check-further-callbacks = false
run = 'move-node-to-workspace R'

[[on-window-detected]]
if.app-id = 'com.apple.mail'
check-further-callbacks = false
run = 'move-node-to-workspace P'

[[on-window-detected]]
if.app-id = 'md.obsidian'
check-further-callbacks = false
run = 'move-node-to-workspace O'

[[on-window-detected]]
if.app-id = 'com.apple.iCal'
check-further-callbacks = false
run = 'move-node-to-workspace T'

[bar]
enabled = true
height = 28
font-size = 14
modules-left = ['workspaces', 'mode', 'focused']
modules-right = ['cpu', 'gpu', 'network', 'memory', 'battery', 'volume', 'clock']
hide-empty-workspaces = true
focused-show-icon = true

# Digit workspaces → kanji labels
[bar.workspace-symbols]
0 = '〇'
1 = '一'
2 = '二'
3 = '三'
4 = '四'
5 = '五'
6 = '六'
7 = '七'
8 = '八'
9 = '九'

[focus-follows-mouse]
enabled = false

# Raycast's launcher is an overlay, not a window to manage. 'ignore' classifies it as a popup, so
# it is never placed in the tree — which also means no border, since borders are drawn per managed
# window. App-specific knowledge lives here in config, never as a branch in the classifier.
[[window-detection-rules]]
if.app-id = 'com.raycast.macos'
treat-as = 'ignore'
